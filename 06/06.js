document.addEventListener("DOMContentLoaded", ()=>{
    //버튼가져오기
    const img = document.querySelectorAll("section > div > img");
    const bts = document.querySelectorAll("section button");

    for(let be bt of bts){
        console/log(bt.innerHTML);
        bt.addEventListener("click" , () => {
            console.log(bt.innerHTML.slice(-1));

            const nc = Math.floor(Math.random()*6)+1;
            const n = bt.innerHTML.slice(-1); //문자를 숫자로 변환 

            imgs[0].setAttribute("src", `./`)
        })
    };



const handleClick = () => {
    const bts = document.querySelectorAll("");

    bts.forEach(item) =>{
       const n = Math.floor(Math.random()*6)+1;
       item.
    }}
});

